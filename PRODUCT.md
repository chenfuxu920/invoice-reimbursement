# Product

<!-- impeccable:product-schema 1 -->

## Platform

web（Tauri 2 桌面壳内的 WebView，Windows 优先）

## Users

个人出差报销用户——主要使用者是开发者本人，出差回来后用它在本地整理发票和支付账单、自动生成报销材料。偶尔会把界面或导出的报销表展示给同事、上级。

## Product Purpose

把个人出差报销从"手工对账 + 手填表单"变成自动化：发票 OCR 识别、微信/支付宝账单自动匹配、按出差行程分趟归类、一键生成报销表单与对照 PDF。

## Positioning

本地运行的桌面工具：发票与账单数据不出本机；识别、匹配、分趟、导出全流程自动化，最终产出可直接提交的报销表与对照 PDF。

## Operating Context

- Windows 桌面应用（Tauri），窗口可缩放，窄窗口下布局必须可用。
- 核心流程：导入发票（PDF/图片，OCR 识别）→ 导入账单（微信/支付宝 xlsx/csv）→ 自动匹配 → 分趟 → 导出报销表 + 对照 PDF。
- 全程中文界面。
- 存在调试界面（PDF 文字提取、三引擎坐标对比），属于高级工具区。

## Capabilities and Constraints

- 功能：发票 OCR（PaddleOCR）、PDF 文字提取（pdfplumber fork）、发票/账单自动匹配、按城市与日期分趟、每趟报销表单编辑、批量导出、待调整票据人工归趟、调试工具。
- 技术栈：Vue 3 + Vite + Tailwind CSS 4 + Pinia + Tauri 2（Rust）。当前无组件库，组件全部用 Tailwind 手写。
- 约束（用户确认）：保持中文界面；兼容窄窗口。
- 未定决策：重设计是否引入组件库；功能流程允许小幅优化，但核心能力与数据模型不变。

## Brand Commitments

- 产品名：发票报销助手
- 作者署名：白开水
- 版本：v0.5.0
- 界面语言：中文

## Evidence on Hand

- 现有前端：5 个视图（首页/导入/匹配/导出/调试）+ 若干组件，Tailwind 灰底白卡 + 蓝色主色的朴素风格，含 emoji 图标、alert() 提示。
- 真实业务数据：发票（增值税电子发票/行程单/火车票）、微信/支付宝账单、分趟行程。
- 后端能力齐全：OCR、解析、匹配、分趟、导出均可用。

## Product Principles

1. 本地优先——数据不出本机，界面服务于本地桌面场景。
2. 自动化为主，人工兜底——识别/匹配/分趟自动完成，但每一步结果可见、可手动调整。
3. 准确优先——金额、日期、行程是报销核心事实，任何不确定都要显式标出并可修正。
4. 透明可追溯——匹配依据、分趟归属、待调整项都要让用户看得懂。

## Accessibility & Inclusion

窄窗口兼容是已确认需求；桌面应用应支持键盘可达与清晰的状态反馈（成功/失败/待处理）。
